#include <stdio.h>
int main(void)
{

int num1,num2,total;
        printf("Enter two numbers \n");
        scanf("%d %d", &num1, &num2);
        total=num1+num2;
        printf("The total of these two numbers is %d", total);
        return 0;
}
